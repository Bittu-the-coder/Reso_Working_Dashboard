import React from "react";
import CollaborativeTasks from "../../components/tasks/CollaborativeTasks";

const TasksPage: React.FC = () => {
  return <CollaborativeTasks />;
};

export default TasksPage;
