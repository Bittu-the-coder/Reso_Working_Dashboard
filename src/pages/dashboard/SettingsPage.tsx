import Settings from "../../components/Settings";

const SettingsPage: React.FC = () => {
  // The Settings component handles its own state and functionality
  return <Settings />;
};

export default SettingsPage;
